{% extends 'base.html.twig' %}
  
{% block title %}New Club{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Create Club</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_club_index') }}"> 
                    View All Clubs
                </a>
            </div>
            <div class="card-body">
                 {{ include('club/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}