
  
{% extends 'base.html.twig' %}
{% block title %}Edit Club{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Club</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_club_index') }}"> 
                    View All Club
                </a>
            </div>
            <div class="card-body">
                {% include 'Club/_form.html.twig' with {'button_label': 'Update Club', 'button_color': 'btn-outline-success'}  %} 
                  <a href="{{ path('app_club_index') }}">back to list</a>

                  {{ include('club/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
  
{% endblock %}
